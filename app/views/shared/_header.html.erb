<nav class="navbar navbar-dark bg-inverse navbar-inverse">
  <%= link_to "Welcome on point review portal", root_path, class: "navbar-brand active" %>
  <ul class="nav navbar-nav">

    <li class="nav-item active">
      <% if user_signed_in? %>
        <% if (current_user.has_role? :admin) %>
          <a class="nav-link" href="">Users<span class="sr-only">(current)</span></a>
        <% else %>
          <a class="nav-link" href="">My Points <span class="sr-only">(current)</span></a>
        <% end %>
      <% end %>
    </li>
    <li class="nav-item">
      <a class="nav-link" href=""></a>
    </li>
  </ul>
  <ul class="nav navbar-nav navbar-right">
    <% if user_signed_in? %>
      <li><a href="/users/sign_out" method="DELETE">Sign out &nbsp;</a></li>
    <% else %>
      <% if request.path == "/users/sign_in" %>
        <li><a href="/users/sign_up">Sign up&nbsp;</a></li>
      <% else %>
        <li><a href="/users/sign_in">Log in &nbsp;</a></li>
      <% end %>  
    <% end%>
  </ul>
</nav>
